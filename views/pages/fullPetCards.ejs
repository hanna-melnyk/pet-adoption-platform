<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../style.css" />
    <title>Full pet card </title>
</head>
<body>
     <div class="container">
        <a href="/home">Home</a>

        <div>
            <% if (pet) { %>
                <img
              src="<%= pet.photos[0] %>"
              alt="cat image"
              style="width: 500px;"
            />
              <h3 class="card-title"><%= pet.name %></h3>
              <p><%= pet.species %></p>
              <p><%= pet.breed %></p>
              <p><%= pet.age %></p>
              <p><%= pet.color %></p>
              <p><%= pet.gender %></p>
              <p class="card-text"><%= pet.description %></p>
              <p><%= pet.adoptionStatus %></p>
            <% } else { %>
                <p>Pet not found</p>
            <% } %>
        </div>

        <form action="/home/delete-pet/<%= pet.id %>" method="post">
            <button>Delete</button>
            <a href="/home/update-petCard/<%= pet.id %>">Edit</a>
        </form>
     </div>
    
</body>
</html>