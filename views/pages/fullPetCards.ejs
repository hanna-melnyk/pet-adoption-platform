<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="../../style.css" />
    <title>Full pet card </title>
</head>
<body>
  <%- include('header') -%>
     <div class="container">
        <div class="full-card-container">
            <% if (pet) { %>
                 <% if (pet.photos && pet.photos.length > 0) { %>
            <img
                src="/uploads/<%= pet.photos[0].split('\\').pop() %>"
                alt="pet image"
                style="width: 300px;"
                class="card-img-full"
            />
        <% } else { %>
            <img
                src="../../images/Pets&children.png"
                alt="pet image"
                style="width: 300px;"
                class="card-img-full"
            />
              <% } %>
              <div class="full-info">
                <div class="card-list-li">
                    <h3 class="card-title"><%= pet.name %></h3>
                    <p class="value"><%= pet.age %></p>
                </div>

                <div class="card-list-li">
                     <div class="card-full-btn">
                <p class="value"><%= pet.breed %></p>
                    <p class="property">breed</p>
                     </div>
                    <div class="card-full-btn">
                    <p class="value"><%= pet.species %></p>
                    <p class="property">species</p>
                    </div>
                    
                    <div class="card-full-btn">
                    <p class="value"><%= pet.color %></p>
                    <p class="property">color</p>
                    </div>
                    
                    <div class="card-full-btn">
                    <p class="value"><%= pet.gender %></p>
                    <p class="property">gender</p>
                    </div>
                   
                </div>
                <p class="card-text description-full"><%= pet.description %></p>
              <div class="card-list-li">
                 <div class="card-full-bottom">
                        <p class="value"><%= pet.adoptionStatus %></p>
                        <p class="property">adoption status</p>
                 </div>
                 <div class="card-full-bottom">
                        <p class="value">price</p>
                        <p class="property">price</p>
                 </div>
            <% } else { %>
                <p>Pet not found</p>
            <% } %>
               </div>
              <form action="/home/delete-pet/<%= pet.id %>" method="post" class="update-form">
            <button class="btn-delete">Delete</button>
                <a href="/home/update-petCard/<%= pet.id %>" class="btn-update">Edit</a>
        </form>
         </div>
     </div>
    </div>
</body>
</html>