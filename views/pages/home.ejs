<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../../style.css">
    <title>Home page</title>
</head>
<body>
    <main class="container">
        <a href="/home/addNewPet">Add new pet</a>
        <section class="filter">
            <h1>Search for a pet</h1>
            <form action="">
                <input type="text" placeholder="search">
                <button>Search</button>
             </form>
             <button>Cats</button>
             <button>Dogs</button>
             <button>Parrots</button>
             <button>Fish</button>
            <button> Insects</button>
            <button>Other pets</button>
        </section>  
        
        
        <section class="cards">
            <ul class="row row-cols-1 row-cols-md-3 g-5">
                
<% for (let i = 0; i < pets.length; i++) {%>
    <li class="col">
    <img src="<%= pets[i].photos[0] %>" alt="cat image"  class="card-img-top" style="width:200px; height:200px; object-fit:cover;" >
    <div class="card-body">
<h3 class="card-title"><%= pets[i].name %></h3>
    <p><%= pets[i].species %></p>
    <p><%= pets[i].breed %></p>
    <p><%= pets[i].age %></p>
    <p><%= pets[i].color %></p>
    <p><%= pets[i].gender %></p>
    <p class="card-text"><%= pets[i].description %></p>
    <p><%= pets[i].adoptionStatus %></p>
    </div>
    
 </li>
<% } %>
               
            </ul>

        </section>   
    </main>
</body>
</html>